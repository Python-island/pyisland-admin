<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pyisland.server.user.payment.mapper.PaymentNotifyLogMapper">

    <insert id="insertIgnore">
        INSERT IGNORE INTO payment_notify_log
        (notify_id, out_trade_no, event_type, verify_ok, process_status, raw_body, created_at)
        VALUES
        (#{notifyId}, #{outTradeNo}, #{eventType}, #{verifyOk}, #{processStatus}, #{rawBody}, #{createdAt})
    </insert>

</mapper>

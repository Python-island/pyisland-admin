<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pyisland.server.servicestatus.mapper.ServiceStatusMapper">

    <resultMap id="ServiceStatusResultMap" type="ServiceStatus">
        <id property="id" column="id"/>
        <result property="apiName" column="api_name"/>
        <result property="status" column="status"/>
        <result property="message" column="message"/>
        <result property="remark" column="remark"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="selectByApiName" resultMap="ServiceStatusResultMap">
        SELECT id, api_name, status, message, remark, updated_at
        FROM service_status
        WHERE api_name = #{apiName}
    </select>

    <select id="selectAll" resultMap="ServiceStatusResultMap">
        SELECT id, api_name, status, message, remark, updated_at
        FROM service_status
        ORDER BY id ASC
    </select>

    <update id="update">
        UPDATE service_status
        SET status = #{status},
            message = #{message},
            remark = #{remark},
            updated_at = #{updatedAt}
        WHERE api_name = #{apiName}
    </update>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO service_status (api_name, status, message, remark, updated_at)
        VALUES (#{apiName}, #{status}, #{message}, #{remark}, #{updatedAt})
    </insert>

</mapper>

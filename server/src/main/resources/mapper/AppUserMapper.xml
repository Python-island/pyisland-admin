<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pyisland.server.repository.AppUserMapper">

    <resultMap id="AppUserResultMap" type="AppUser">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="avatar" column="avatar"/>
        <result property="sessionToken" column="session_token"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="selectByUsername" resultMap="AppUserResultMap">
        SELECT id, username, email, password, avatar, session_token, created_at
        FROM app_user
        WHERE username = #{username}
    </select>

    <select id="selectByEmail" resultMap="AppUserResultMap">
        SELECT id, username, email, password, avatar, session_token, created_at
        FROM app_user
        WHERE email = #{email}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO app_user (username, email, password, avatar, created_at)
        VALUES (#{username}, #{email}, #{password}, #{avatar}, #{createdAt})
    </insert>

    <select id="selectAll" resultMap="AppUserResultMap">
        SELECT id, username, email, password, avatar, session_token, created_at
        FROM app_user
        ORDER BY created_at DESC
    </select>

    <delete id="deleteByUsername">
        DELETE FROM app_user
        WHERE username = #{username}
    </delete>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM app_user
    </select>

    <update id="updateProfile">
        UPDATE app_user
        SET password = #{password},
            avatar = #{avatar}
        WHERE username = #{username}
    </update>

    <update id="updateAvatar">
        UPDATE app_user
        SET avatar = #{avatar}
        WHERE username = #{username}
    </update>

    <update id="updateSessionToken">
        UPDATE app_user
        SET session_token = #{sessionToken}
        WHERE username = #{username}
    </update>

</mapper>

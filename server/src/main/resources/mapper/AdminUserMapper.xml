<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pyisland.server.repository.AdminUserMapper">

    <resultMap id="AdminUserResultMap" type="AdminUser">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="avatar" column="avatar"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="selectByUsername" resultMap="AdminUserResultMap">
        SELECT id, username, password, avatar, created_at
        FROM admin_user
        WHERE username = #{username}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO admin_user (username, password, avatar, created_at)
        VALUES (#{username}, #{password}, #{avatar}, #{createdAt})
    </insert>

    <select id="selectAll" resultMap="AdminUserResultMap">
        SELECT id, username, password, avatar, created_at
        FROM admin_user
        ORDER BY created_at DESC
    </select>

    <delete id="deleteByUsername">
        DELETE FROM admin_user
        WHERE username = #{username}
    </delete>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM admin_user
    </select>

    <update id="updateProfile">
        UPDATE admin_user
        SET password = #{password},
            avatar = #{avatar}
        WHERE username = #{username}
    </update>

    <update id="updateAvatar">
        UPDATE admin_user
        SET avatar = #{avatar}
        WHERE username = #{username}
    </update>

</mapper>
